"""Prompts for the Control (inspection) module LLM calls."""

SYSTEM_PHOTO_ANALYSIS = """
Ты — эксперт строительного контроля. Твоя задача: проанализировать результаты CV-детекции дефектов \
строительных конструкций и выдать структурированное заключение в формате JSON.

ВАЖНО:
- Все заключения носят ПРЕДВАРИТЕЛЬНЫЙ характер и требуют проверки специалистом.
- Ссылайся только на действующие нормативные документы (СП, ГОСТ, СНиП).
- Классифицируй дефекты строго по трём категориям: acceptable, significant, critical.
- critical — требует немедленного устранения и уведомления ответственного лица.
""".strip()

PHOTO_ANALYSIS_PROMPT = """
Объект строительного контроля: {construction_type}
Результаты автоматической CV-детекции:
{yolo_results}

Связанные нормативные требования из базы НТД:
{ntd_context}

Верни JSON строго в следующем формате:
{{
  "construction_type": "<тип конструкции>",
  "construction_type_confidence": <0.0-1.0>,
  "defects": [
    {{
      "defect_type": "<тип дефекта>",
      "severity": "acceptable|significant|critical",
      "description": "<подробное описание>",
      "measurement_mm": <null или число>,
      "confidence": <0.0-1.0>,
      "ntd_references": [
        {{"code": "<СП/ГОСТ>", "clause": "<номер пункта>", "requirement": "<краткое требование>"}}
      ]
    }}
  ],
  "overall_assessment": "<общий вывод>",
  "requires_immediate_action": <true|false>,
  "disclaimer": "Заключение носит предварительный характер и требует проверки специалистом."
}}
""".strip()

ESCALATION_PROMPT = """
Первичный анализ дефектов показал низкую уверенность или критические дефекты. \
Выполни расширенный анализ с учётом всех деталей фото.

Первичное заключение:
{initial_verdict}

Дополнительный контекст из НТД:
{ntd_context}

Уточни и подтверди или опровергни первичный вердикт, выдай обновлённый JSON того же формата.
""".strip()
